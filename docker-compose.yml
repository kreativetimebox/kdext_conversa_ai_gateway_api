version: '3.8'

services:
  voice-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: voice-gateway
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0
      - PORT=8001
